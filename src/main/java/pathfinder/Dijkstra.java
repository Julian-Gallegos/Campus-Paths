package pathfinder;

import graph.Graph;

import java.util.PriorityQueue;

public class Dijkstra<E> {

    // Not an ADT

    public void search(E starting_node, E destination_node, Graph<E,Double> graph) {

        PriorityQueue<E> active = new PriorityQueue<>();
        
    }
}
