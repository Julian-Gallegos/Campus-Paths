# Tests that nodeExists properly returns true if the node exists

#Create an empty graph
created graph graph1

# Add a node
added node node1 to graph1

# See if node1 exists
node1 in graph1 exists(true)