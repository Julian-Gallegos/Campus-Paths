# Tests that edgeExists properly returns true if the edge exists

# Create an empty graph
CreateEmptyGraph graph1

# Add a pair of nodes
AddNode graph1 node1
AddNode graph1 node2

# Add an edge from node1 to node 2
AddEdge graph1 node1 node2 edge1

# See if edge1 exists
EdgeExists graph1 node1 node2 edge1